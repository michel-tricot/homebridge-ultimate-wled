{
  "pluginAlias": "Ultimate Wled",
  "pluginType": "platform",
  "singular": true,
  "strictValidation": false,
  "schema": {
    "title": "WLED Plugin Configuration",
    "type": "object",
    "properties": {
      "wleds": {
        "type": "array",
        "title": "wleds",
        "items": {
          "type": "object",
          "title": "WLED Configuration",
          "required": [
            "name",
            "ip"
          ],
          "properties": {
            "name": {
              "title": "WLED name",
              "description": "Name for this WLED instance.",
              "type": "string",
              "placeholder": "Enter WLED name"
            },
            "ip": {
              "title": "WLED IP address",
              "description": "IP address for this WLED instance.",
              "type": "string",
              "placeholder": "Enter WLED IP address"
            }
          }
        }
      }
    }
  }
}
